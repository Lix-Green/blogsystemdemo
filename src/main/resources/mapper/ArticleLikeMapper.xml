<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.blogsystemdemo.mapper.ArticleLikeMapper"> <!-- 对应Mapper接口全路径 -->

    <!-- 1. 新增点赞记录 -->
    <insert id="insertLike" parameterType="com.example.blogsystemdemo.entity.ArticleLike">
        INSERT INTO article_like (article_id, user_id, create_time)
        VALUES (#{articleId}, #{userId}, NOW())
    </insert>

    <!-- 2. 删除点赞记录（取消点赞） -->
    <delete id="deleteLike">
        DELETE
        FROM article_like
        WHERE article_id = #{articleId}
          AND user_id = #{userId}
    </delete>

    <!-- 3. 判断用户是否已点赞 -->
    <select id="checkLike" resultType="java.lang.Integer">
        SELECT COUNT(1)
        FROM article_like
        WHERE article_id = #{articleId}
          AND user_id = #{userId}
    </select>

    <!-- 4. 统计文章点赞数 -->
    <select id="countLikeByArticleId" resultType="java.lang.Integer">
        SELECT COUNT(1)
        FROM article_like
        WHERE article_id = #{articleId}
    </select>

</mapper>
