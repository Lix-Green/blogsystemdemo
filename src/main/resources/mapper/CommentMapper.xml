<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.blogsystemdemo.mapper.CommentMapper">
    <insert id="createComment" parameterType="Comment" useGeneratedKeys="true" keyProperty="id">
        insert into comment (article_id, user_id, content, parent_id, created_time, updated_time)
        values (#{articleId}, #{userId}, #{content}, #{parentId}, now(), now())
    </insert>
    <delete id="deleteCommentById" parameterType="Long">
        delete
        from comment
        where id = #{id}
    </delete>
    <select id="listCommentsByArticleId" resultType="Comment">
        select *
        from comment
        where article_id = #{articleId}
        order by created_time desc
    </select>
    <select id="getCommentById" resultType="Comment">
        select *
        from comment
        where id = #{id}
    </select>
    <update id="updateArticleCommentCount">
        update article
        set comment_count = #{count}
        where id = #{articleId}
    </update>
</mapper>