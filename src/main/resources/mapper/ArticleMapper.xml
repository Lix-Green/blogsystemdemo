<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.blogsystemdemo.mapper.ArticleMapper">
    <select id="getAllArticles" resultType="com.example.blogsystemdemo.entity.Article">
        select *
        from article
        order by created_time desc
        limit #{pageNum}, #{pageSize}
    </select>

    <insert id="createArticle" parameterType="com.example.blogsystemdemo.entity.Article">
        insert into article (title, content, summary, cover_image, author_id,
                             view_count, comment_count, status, created_time, updated_time)
        values (#{title}, #{content}, #{summary}, #{coverImage}, #{authorId}, 0, 0, #{status}, now(), now())
    </insert>

    <update id="updateArticle" parameterType="com.example.blogsystemdemo.entity.Article">
        update article
        set title        = #{title},
            content      = #{content},
            summary      = #{summary},
            cover_image  = #{coverImage},
            author_id    = #{authorId},
            status       = #{status},
            updated_time = now()
        where id = #{id}
    </update>

    <delete id="deleteArticle" parameterType="Long">
        delete
        from article
        where id = #{id}
    </delete>

    <select id="getArticleById" parameterType="Long" resultType="com.example.blogsystemdemo.entity.Article">
        select *
        from article
        where id = #{id}
    </select>

    <update id="incrementViewCount" parameterType="Long">
        UPDATE article
        SET view_count = view_count + 1
        WHERE id = #{id}
    </update>

    <select id="getTotalArticles" resultType="int">
        SELECT COUNT(*)
        FROM article
    </select>

    <!-- 1. 按阅读量降序查询热门文章（阅读量相同则按发布时间降序） -->
    <select id="getHotArticles" resultType="com.example.blogsystemdemo.entity.Article">
        SELECT *
        FROM article
        ORDER BY view_count DESC, created_time DESC -- 核心：先按阅读量降序，再按发布时间降序
        LIMIT #{pageNum}, #{pageSize} -- 分页：与最新文章分页逻辑一致
    </select>

    <!-- 2. 获取热门文章总数（直接复用getTotalArticles的逻辑，语义更清晰） -->
    <select id="getTotalHotArticles" resultType="int">
        SELECT COUNT(*)
        FROM article
    </select>

    <update id="updateLikeCount">
        UPDATE article
        SET like_count = like_count + #{num},
        updated_time = NOW()  <!-- 同步更新文章修改时间 -->
        WHERE id = #{articleId}
    </update>

    <!-- 新增：更新文章收藏数 -->
    <update id="updateCollectionCount">
        UPDATE article
        SET collection_count = collection_count + #{num},
            updated_time     = NOW()
        WHERE id = #{articleId}
    </update>

    <select id="getArticlesByAuthor" resultType="Article">
        SELECT *
        FROM article
        WHERE author_id = #{authorId}
        ORDER BY created_time DESC
        LIMIT #{pageNum}, #{pageSize}
    </select>

    <select id="countArticlesByAuthor" resultType="int">
        SELECT COUNT(1)
        FROM article
        WHERE author_id = #{authorId}
    </select>

</mapper>